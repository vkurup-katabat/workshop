spring:
  cloud:
    inetutils:
      ignored-interfaces:
        - tun.*
        - eno.*
        - virbr.*
eureka:
  instance:
    preferIpAddress: false

server:
  port: 8091

security:
  basic:
    enabled: false

management:
  security:
    enabled: false

logging:
  file: customers-service.log